/**
 * Gandharva API configuration.
 *
 * All network requests from the Expo app must use this base URL.
 * Never hardcode backend URLs anywhere else.
 *
 * Replace the placeholder below with your actual Vercel deployment URL.
 */
export const API_BASE = 'https://gandarva-scraper.vercel.app';
